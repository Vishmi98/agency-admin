export enum UserRole {
  Admin = 1,
  Teacher = 2,
  Student = 3,
}

export enum UserRole {
  admin = 1,
  consultant = 2,
  hr = 3,
  accounts = 4,
  ceo = 5,
  marketingManager = 6,
  branchManager = 7,
  operationManager = 8,
  coordinator = 9,
  juniorConsultant = 10,
}

export enum AuthProvider {
  Local = 1,
  Google = 2,
  Facebook = 3,
}

export enum VerifyType {
  Email = 1,
  Mobile = 2
}

